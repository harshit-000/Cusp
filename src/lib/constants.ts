/** The single implicit user in Phase 1–2. Multi-user replaces this with real rows. */
export const SELF_USER_ID = "self";

/** Applied roles with no reply after this many days are auto-marked "ghosted". */
export const GHOST_DAYS = 21;
